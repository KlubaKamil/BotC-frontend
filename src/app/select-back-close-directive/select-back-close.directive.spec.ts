import { SelectBackCloseDirective } from './select-back-close.directive';

describe('SelectBackCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectBackCloseDirective();
    expect(directive).toBeTruthy();
  });
});
